<form class="{{cssClass}}" autocomplete="off">
  <header class="sheet-header">
    <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}" />
    <div class="header-fields">
      <h1 class="charname"><input name="name" type="text" value="{{item.name}}" placeholder="Name" /></h1>
    </div>
  </header>

  <section>
    <table>
      <tr>
        <th>{{localize "swSaga.talent.name" }}</th>
        <th>{{localize "swSaga.talent.class" }}</th>
        <th>{{localize "swSaga.talent.talentTree" }}</th>
        <th>{{localize "swSaga.talent.description" }}</th>
        <th>{{localize "swSaga.talent.prerequisite" }}</th>
      </tr>
      <tr>
        <td>
          <input name="data.name" type='text' value="{{data.name}}"  />
        </td>
        <td>
          <input name="data.class" type='text' value="{{data.class}}"  />
        </td>
        <td>
          <input name="data.talentTree" type='text' value="{{data.talentTree}}"  />
        </td>
        <td>
          <input name="data.description" type='text' value="{{data.description}}"  />
        </td>
        <td>
          <input name="data.prerequisite" type='text' value="{{data.prerequisite}}"  />
        </td>
      </tr>
    </table>

  </section>
</form>