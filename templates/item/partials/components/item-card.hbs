<div class='item-card item draggable' data-item-id="{{_id}}" >
  <a class='item-edit' title="{{localize "swsaga.sheet.editItem"}}">
    {{log weapon}}
    <p>{{name}}</p>
    <img src="{{image}}">
    <p>{{data.damage}}</p>
  </a>
</div>